<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="GymWorkoutAppUI.Views.AddWorkoutPage"
    Title="Log Workout"
    BackgroundColor="#0A0A0A">

    <Shell.TitleView>
        <Label
            Text="LOG WORKOUT"
            TextColor="#FFD700"
            FontAttributes="Bold"
            FontSize="16"
            CharacterSpacing="3"
            VerticalOptions="Center" />
    </Shell.TitleView>

    <ScrollView>
        <VerticalStackLayout Padding="24,16,24,32" Spacing="20">

            <BoxView Color="#FFD700" HeightRequest="3" Margin="-24,0" />

            <Label Text="WORKOUT DETAILS" FontSize="11" TextColor="#FFD700" CharacterSpacing="4" FontAttributes="Bold" Margin="0,16,0,0" />

            <VerticalStackLayout Spacing="6">
                <Label Text="WORKOUT NAME" FontSize="10" TextColor="#666666" CharacterSpacing="2" />
                <Border BackgroundColor="#161616" StrokeThickness="1" Stroke="#2A2A2A" StrokeShape="RoundRectangle 8">
                    <Entry
                        x:Name="WorkoutNameEntry"
                        Placeholder="e.g. Upper Body, Leg Day..."
                        PlaceholderColor="#444444"
                        TextColor="White"
                        BackgroundColor="Transparent"
                        HeightRequest="52"
                        Margin="14,0" />
                </Border>
            </VerticalStackLayout>
            
            <VerticalStackLayout Spacing="6">
                <Label Text="DATE" FontSize="10" TextColor="#666666" CharacterSpacing="2" />
                <Border BackgroundColor="#161616" StrokeThickness="1" Stroke="#2A2A2A" StrokeShape="RoundRectangle 8">
                    <DatePicker
                        x:Name="WorkoutDatePicker"
                        Format="MMMM dd, yyyy"
                        TextColor="White"
                        BackgroundColor="Transparent"
                        HeightRequest="52"
                        Margin="14,0" />
                </Border>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="6">
                <Label Text="NOTES (OPTIONAL)" FontSize="10" TextColor="#666666" CharacterSpacing="2" />
                <Border BackgroundColor="#161616" StrokeThickness="1" Stroke="#2A2A2A" StrokeShape="RoundRectangle 8">
                    <Editor
                        x:Name="NotesEditor"
                        Placeholder="What did you work on? Any PRs?"
                        PlaceholderColor="#444444"
                        TextColor="White"
                        BackgroundColor="Transparent"
                        HeightRequest="120"
                        Margin="14,8" />
                </Border>
            </VerticalStackLayout>

            <Label x:Name="MessageLabel" FontSize="13" HorizontalOptions="Center" IsVisible="False" />

            <Button
                x:Name="SaveButton"
                Text="SAVE WORKOUT"
                HeightRequest="56"
                BackgroundColor="#FFD700"
                TextColor="#0A0A0A"
                FontAttributes="Bold"
                FontSize="15"
                CharacterSpacing="3"
                CornerRadius="10"
                Clicked="OnSaveWorkoutClicked"
                Margin="0,12,0,0" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
